# Orders list → select orders → "More actions" → Add India Post tracking.
# Opens the app with just the selected orders listed, ready for tracking numbers.
[[extensions]]
name = "Add India Post tracking"
description = "Enter India Post tracking numbers for the selected orders"
handle = "book-orders-indiapost"
type = "admin_link"

  [[extensions.targeting]]
  target = "admin.order-index.selection-action.link"
  url = "/"
