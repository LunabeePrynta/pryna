{
  "name": "indiapost-shipping-shopify-app",
  "version": "0.1.0",
  "private": true,
  "description": "Shopify app that books orders with India Post, generates address labels and syncs tracking to Shopify.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/server.js",
    "dev": "node --disable-warning=ExperimentalWarning --watch src/server.js",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "workspaces": [
    "extensions/*"
  ]
}
